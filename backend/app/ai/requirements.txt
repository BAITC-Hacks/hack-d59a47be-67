# Optional AI transport, using exactly the pins already verified in backend dev.
# Install in addition to root requirements.lock; do not replace the backend lockfile.
httpx==0.28.1
httpcore==1.0.9
certifi==2026.7.22

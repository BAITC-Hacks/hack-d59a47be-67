# Готовность Career Quest — 2026-09-23

> Историческая проверка до появления PR №5. Актуальная проверка общей ветки, нового Docker-образа и HR-аналитики — [PRODUCT_REPORT.md](PRODUCT_REPORT.md). Ниже сохранены факты прежних refs, а не текущий список незавершённых функций.

**Сценарий сотрудника работает. К полной сдаче по ТЗ проект пока не готов:** отсутствуют три обязательных HR-среза, а проверенные компоненты ещё находятся в отдельных ветках. Это проверка готовности всего продукта, а не только совместимости его модулей.

## Версии и обновление веток

После `git fetch --all --prune` новых коммитов не оказалось: все семь локальных tracking-веток совпадали с origin. Проверены:

| Компонент | Ветка | Commit |
|---|---|---|
| Backend | `codex/backend-ai-handoff` | `3b85babc78e6c522b6c5d04d42e5a16a37966570` |
| AI | `codex/ai-recommendations` | `f4abd4a5d445d79972c2ca74aea949dc6e8f6e3a` |
| Frontend | `feat/frontend-app` | `f91638b612dfc36b06b399fdbb426f51c1ad248c` |
| Основа | `codex/architecture-foundation` | `e3dfc7e` |
| Сдаваемая main | `main` | `779ec87` |

Backend и AI объединены только в экспортированную временную копию, виртуальное дерево `0721c877e22e2504d60f287fbe18ad427ac1a75c`; frontend добавлен из указанного commit. Командные ветки и PR не сливались. Прямое пробное объединение AI и frontend обнаруживает конфликт журнала `AGENTS.md`; при согласованной интеграции нужно сохранить обе записи. Отчёт и его публикация меняют только документацию AI и общий журнал, не проверенный runtime.

## Новая проверка штатного запуска

В экспортированной копии выполнен `pnpm start` из `frontend/`: команда поднимает backend и Vite вместе. Перед запуском установлены зависимости и подготовлены отдельная синтетическая БД, учётные записи и переменные окружения. Это проверка штатного запуска после подготовки, а не готовой установки из чистого клона.

Среда: macOS, Python 3.12.11, Node 22.14.0, pnpm 11.19.0; `pnpm install --offline --frozen-lockfile` прошёл. Использованы вымышленные fixtures frontend и ранее разрешённый локальный OpenAI ключ. `AI_ENABLED=true`, модель `gpt-4.1-mini-2025-04-14`, deadline 6 s. Кнопка просмотра готового демо не использовалась.

| Браузерная проверка | Фактический результат |
|---|---|
| Вход сотрудника → заголовок кабинета | PASS, 211 ms в одном наблюдении |
| UI → API → настоящий OpenAI | HTTP 200, `ok/oleg`, три карточки, 2959 ms от клика до получения JSON |
| Объяснения | Русский текст с целью, разрывом, критичностью, историей и доступностью; первым выбран системный дизайн |
| Preview → обычное завершение вымышленной активности | `UI_SYSTEMS`: 2 → 3, прогресс 56,25% → 62,5%, revision 1 → 2 |
| Актуальность рекомендаций | После завершения `latest.stale=true`, обновлённый прогресс отображён в UI |
| Права | Сотрудник получает 403 на HR summary; отдельный HR-вход получает 200 |
| HR-экран | Открывается, отображает каталог сотрудников и четыре счётчика; обязательных сводок нет |

Результат настоящего HR API на тестовой базе: `employee_count=3`, `employees_with_goal=2`, `completion_count=2`, `demo_simulation_count=0`. Наличие цели не означает наличие рекомендованного шага; общее число завершений не является участием по активностям.

В начальном проверочном окружении ссылка на зависимости другой временной копии вызвала 403 для шрифтов Vite. После отдельной установки зависимостей ошибка исчезла, исходники приложения не менялись. В итоговом прогоне браузер сообщил только ожидаемые 404 для ещё не существующих latest-рекомендаций и 403 при намеренной проверке прав. Браузер и оба сервера остановлены; временные пароли и БД удалены.

Один замер интерфейса и один AI-запрос не подтверждают p95 или нагрузочный SLA. Требование исходного ТЗ: UI до 2 s, AI до 10 s; эти наблюдения укладываются в соответствующие пределы.

## Какие результаты остаются действительными

Новые commits runtime не появились; hashes AI-файлов совпадают с [latency_verification.json](latency_verification.json). Полные suites повторно не запускались. Сохраняется ранее фактически полученное подтверждение для тех же исходников:

- Совместный backend + AI: **498 тестов**, отдельно **19 handoff checks**, **21 исходный unit test**, **9 schema checks**, Ruff; TCP и Docker build/non-root/allowlist/volume/session/exact replay после пересоздания — PASS. Подробности в [LATENCY_REPORT.md](LATENCY_REPORT.md).
- Live AI на трёх синтетических backend-сценариях по три раза: **9/9**, медиана 1106,57 ms, максимум 1284,37 ms. Это ограниченная рубрика, не скрытые профили жюри.
- Тот же frontend `f91638b`: **52 теста**, сборка, форматирование, OpenAPI parity и HTTP smoke — PASS; предыдущая браузерная проверка размеров 320/390/1440 и сохранения после restart — в [SYSTEM_REPORT.md](SYSTEM_REPORT.md). В ней использована предыдущая AI-версия; совместимость текущего AI с UI дополнительно проверена новым сценарием выше.
- Проверка Docker использует тестовый selector. Последняя live-серия и новый браузерный AI-запрос выполнены на Mac; их нельзя выдавать за повторную live-проверку текущего образа.

## Сверка с обязательными требованиями

Источник — [исходное ТЗ](../../../docs/requirements/original-spec.txt), раздел 7, строки 64–76.

| Требование | Статус и граница подтверждения |
|---|---|
| Профиль, роль/грейд, навыки, цель, история | Реализовано; профиль открыт в новом браузерном прогоне, остальные переходы проверены ранее |
| 1–3 многофакторные AI-рекомендации | Реализовано; новый live UI-запрос и прежняя рубрика 9/9 |
| Объяснение минимум по трём факторам | Реализовано; backend facts и связанные evidence, понятные русские карточки |
| Рост навыков и прогресса после выполнения | Реализовано; новый сквозной UI-сценарий PASS, cap/replay ранее покрыты тестами |
| Загрузка дополнительных профилей и истории жюри | Реализовано; preview/atomic commit и права проверены ранее, в этом прогоне импорт не повторялся |
| Простой HR-view из трёх срезов | **Не реализован в требуемом объёме**, см. ниже |

## Что закрыть перед сдачей

### 1. Обязательный HR-view — Алихан и Батыр

ТЗ прямо требует видеть: какие навыки чаще проседают, у кого нет рекомендованного шага, участие по активностям. Опциональность расширенного дашборда в разделе 8 не отменяет эти три must-have.

Backend `CareerService.summary()` (`backend/app/service.py:649–664`) и `HRSummaryResponse` (`backend/app/contracts.py:402–407`) возвращают только четыре общих счётчика. Frontend `frontend/src/features/hr/HrPage.tsx:328` прямо сообщает, что аналитика пробелов и участия не подключена. Это подтверждено кодом, API и [скриншотом реального HR-входа](verification/readiness-hr.png).

**Алихан:** согласовать определения и добавить три агрегата/API/контракт/проверки. Для отсутствия следующего шага различать бизнес-причины, отсутствие ещё выполненного подбора, устаревшую рекомендацию и техническую недоступность AI. **Батыр:** вывести данные и пустые/ошибочные состояния. **Олег:** проверить сводки на согласованных синтетических случаях, включая изменение после completion/import. Это оставшаяся работа, не выполненное исправление.

### 2. Единая версия и инструкция запуска — команда

На момент проверки `main` содержит только начальный двухстрочный README. PR #1, #3 и #4 открыты; #1/#3 — draft. Frontend опубликован веткой, PR для него отсутствует. Совместимость временного экспорта не делает `main` готовой к сдаче.

**Батыр:** подготовить frontend PR. **Алихан как интегратор:** после согласованного review собрать единую ветку, сохранив журналы, и проверить подготовку/запуск из чистого клона. Слияние остаётся отдельным согласованным действием. **Олег:** после интеграции обновить итоговый README и сценарий защиты по проверенным материалам участников.

Корневой README пока содержит устаревшие заявления про отсутствие интегрированного AI, непроверенный Docker и runtime fallback. Фактически AI подключается, Docker уже проверен, при отказе AI действует `unavailable/oleg` без fallback. Frontend README тоже требует сверки с итоговой интеграцией. Эти общие инструкции в данном аудите не переписывались.

`pnpm start` успешно запускает UI + API после подготовки среды. `pnpm start:demo` принудительно отключает AI и не подходит для демонстрации настоящего подбора. Текущий Compose поднимает backend; frontend в образ не включён. Для сдачи нужно выбрать и описать проверенный вариант запуска, а не заявлять, что Compose уже запускает весь продукт.

### 3. GitHub CI — владелец GitHub-аккаунта/организации

Проверена annotation check-run `107174305206` для AI commit `f4abd4a`: job не запущен из-за billing lock. Это внешний блокер автоматических проверок, а не падение выполненных тестов приложения. Владелец устраняет ограничение; после этого нужен настоящий повторный запуск CI. Локальные PASS не заменяют зелёный GitHub check.

Деплой, merge PR и изменения runtime других участников в ходе этой проверки не выполнялись. Для демонстрации сценария сотрудника компоненты готовы; для объявления выполнения всего ТЗ нужно закрыть HR-view и подготовить единую воспроизводимую версию.
